{
  "tasks": {
    "lint": "deno lint",
    "format": "deno fmt",
    "build": "deno task --cwd=apps/ui build",
    "dev": "deno task backend:start & deno task ui:start",
    "backend:start": "cd apps/backend && bash start.sh",
    "backend:stop": "cd apps/backend && bash stop.sh",
    "ui:start": "deno task --cwd=apps/ui dev",
    "schema:generate": "cd apps/backend && powersync generate schema --output-path=../ui/src/schema.ts --output=ts"
  }
}
